"""
1- 使用背景:
    每个大版本完成后需要打个标签

2- 作用:
    1) 记录大版本
    2) 备份大版本代码

3- 命令:
    创建本地标签
        git tag -a 标签名 -m '标签描述'
    推送标签到远程仓库
        git push origin 标签名
    删除本地标签
        git tag -d 标签名
    删除远程仓库标签
        git push origin --delete tag 标签名



"""
